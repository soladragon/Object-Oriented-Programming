{"index.js":"function Bird(name) {\n  this.name = name;\n  this.numLegs = 2;\n}\n\nlet canary = new Bird(\"Tweety\");\nlet ownProps = [];\n// Add your code below this line\n//look for x in canary when x means its properties\nfor (let x in canary){\n  //if canary has its own properties\n  if(canary.hasOwnProperty(x)){\n    ownProps.push(x);\n    console.log(ownProps);\n  }\n}\n\n"}